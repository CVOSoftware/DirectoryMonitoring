<UserControl 
    x:Class="DirectoryMonitoring.Studio.View.Component.MonitoringInfo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:DirectoryMonitoring.Studio.View.Component"
    mc:Ignorable="d" 
    d:DesignHeight="450" 
    d:DesignWidth="800">
    <Grid>
        <DataGrid ItemsSource="{Binding Logs}">
            <!--
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path= FileEvent}" Header="Event"  Width="100"/>
                <DataGridTextColumn Binding="{Binding Path= Path}" Header="Path" Width="*"/>
                <DataGridTextColumn Binding="{Binding Path= Timestamp}" Header="TimeStamp" Width="150"/>
            </DataGrid.Columns>
            -->
        </DataGrid>
    </Grid>
</UserControl>
